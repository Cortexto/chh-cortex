---
name: Claude Code adapter — CHH Cortex
type: adapter
---

# Claude Code — CHH Cortex

1. Open terminal in `chh-cortex/` root
2. Run `claude` — reads `CLAUDE.md` automatically
3. Install plugin from `.claude-plugin/plugin.json`

First prompt:

```
Read ENTRY.md and wiki/partnerships/chh.md. Summarize pilot scope and first task for a new CHH user.
```

For grant work:

```
I am working on jhu-lsri-individual-2026. Boot workbench and draft one section with red-team.
```
